<?php

define('APP_DIR', dirname(__DIR__));  // defines a constant named APP_DIR that holds the path of the parent directory of the current script

// __DIR__ is a special PHP constant that represents the directory of the current script
// dirname(__DIR__) moves one level up from the current script's directory.